# AI 大模型配置（统一走 OpenAI 兼容接口，下面三行需同时提供）
# AI_API_KEY=your_api_key_here
# AI_BASE_URL=https://api.deepseek.com/v1
# AI_MODEL=deepseek-chat

# 数据库配置
DB_HOST=your_db_host_here
DB_PORT=3306
DB_NAME=your_db_name_here
DB_USER=your_db_user_here
DB_PASSWORD=your_db_password_here

# 服务器配置
PORT=5000
NODE_ENV=development

# JWT 密钥
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# 反向代理信任层数：部署在 nginx 等代理之后设为 1（限流/日志取真实客户端 IP）；
# 直连部署设 0，避免伪造 X-Forwarded-For 绕过限流
TRUST_PROXY_HOPS=1

# 跨源白名单（逗号分隔）。生产必须写上前端域名
# 开发环境可含 localhost:5173/3000/5000
CORS_ORIGIN=http://localhost:5173,https://your-domain.com

# 强制 HTTPS：上 TLS 后设 1，非 https 请求 301 跳转（防抓包偷 JWT）；纯 http 站点设 0
FORCE_HTTPS=1

# 写请求签名/防重放：enforce | log（只记日志）| off（关闭）
REQUEST_GUARD=enforce

# QQ 互联登录（在 https://connect.qq.com 创建"网站应用"后获取）
# 三项齐备才会启用 QQ 登录，缺任意一项前端不显示入口、后端拒绝发起授权。
# 硬性前提：回调地址的域名必须已在 QQ 互联后台通过备案校验，否则 QQ 会直接拒绝授权跳转。
QQ_APP_ID=your_qq_app_id_here
QQ_APP_KEY=your_qq_app_key_here
# 必须与 QQ 互联后台填写的回调地址逐字符一致（协议、域名、端口、路径都算）
QQ_REDIRECT_URI=https://your-domain.com/api/auth/qq/callback
# 可选：仅当前端与后端不在同一域名（且无网关代理 /api）时才需配置为前端站点根地址，
# 留空则回调后按相对路径 302 回当前 origin 的根目录
# QQ_FRONTEND_URL=https://your-domain.com

# AI 配置加密密钥（用于 GM 后台存储 AI API Key 的 AES-256-CBC 加密）
# 必须为 64 位十六进制字符串（32 字节），生产环境请使用强随机密钥
# 生成方式：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AI_CONFIG_ENCRYPTION_KEY=replace_with_your_64_hex_chars_key_32_bytes_random

# 服务端错误日志飞书告警 webhook（GM 后台「后台日志」页）
# 飞书群 → 设置 → 群机器人 → 添加机器人 → 自定义机器人 → 复制 webhook 地址
# 留空则不发送告警，其余行为（监控级别、日志源、节流）见 server/config/system_log_viewer.json 的 alert 段
# FEISHU_LOG_ALERT_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/your_hook_id
